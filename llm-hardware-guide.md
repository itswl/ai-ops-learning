# 大模型硬件配置与选型指南

> 创建时间: 2026-05-06

---

## 3.1 硬件配置估算

### 硬件核心配置逻辑

大模型部署的硬件决策有一个核心公式：

```
总成本 = GPU 硬件成本 + 电力/散热成本 + 运维人力成本
性能   = 吞吐（token/s）× 并发数 ÷ 延迟（s）
```

对你来说，围绕三个核心资源做配置：

| 资源 | 决定因素 | 瓶颈表现 |
|---|---|---|
| **显存（VRAM）** | 模型参数 + 量化精度 + 上下文长度 | 显存不够直接跑不起来，OOM |
| **显存带宽** | GPU 型号 | 决定了吞吐上限，带宽不够就慢 |
| **算力（FLOPS）** | GPU 型号 + 数量 | 影响首 token 延迟和并发处理能力 |

**优先级**：显存 > 显存带宽 > 算力。能跑起来是第一位的。

---

### 量化技术与显存优化

#### 为什么需要量化

模型训练时用 FP32（4 字节/参数）或 BF16（2 字节/参数），但推理时可以降精度，显存占用大幅减少。

#### 常见精度对比

| 精度 | 每参数字节 | 70B 模型显存 | 效果损失 | 适用场景 |
|---|---|---|---|---|
| FP32 | 4B | ~280GB | 0% | 训练、研究 |
| BF16/FP16 | 2B | ~140GB | 几乎无损 | 训练、高精度推理 |
| FP8 | 1B | ~70GB | 极小 | H100/B200 推理 |
| INT8 | 1B | ~70GB | 几乎无感知 | 通用推理 |
| INT4（Q4_K_M） | 0.5B | ~40GB | 轻微下降 | **性价比甜点** |
| INT4（GPTQ/AWQ） | 0.5B | ~40GB | 比 Q4_K_M 略好 | 生产环境推荐 |
| INT2 | 0.25B | ~20GB | 明显下降，不推荐 | 极端情况 |

#### 量化方法对比

| 方法 | 原理 | 优点 | 缺点 |
|---|---|---|---|
| **GGUF (llama.cpp)** | 逐层量化，CPU/GPU 混合推理 | 生态成熟，Ollama 默认格式，CPU 友好 | GPU 利用率不如专用方案 |
| **GPTQ** | 基于校准数据的量化，GPU 专属 | 效果比 GGUF 略好，推理快 | 需要校准数据，不灵活 |
| **AWQ** | 激活感知量化，保护重要权重 | 当前效果最好的 INT4 方案 | 相对新，支持模型略少 |
| **bitsandbytes** | 运行时量化，HuggingFace 集成 | 使用最简单，几行代码 | 性能不如前两者 |

#### 实测显存速查表（含 KV Cache）

以 4096 token 上下文为例：

| 模型 | FP16 | INT8 | INT4 | 推荐最低 GPU |
|---|---|---|---|---|
| Qwen2.5-7B | 16GB | 10GB | 6GB | RTX 3060/4060 (12GB) |
| Qwen2.5-14B | 30GB | 18GB | 10GB | RTX 4090 (24GB) |
| Qwen2.5-32B | 66GB | 38GB | 22GB | A100 80G / 2×RTX 4090 |
| Qwen2.5-72B | 148GB | 80GB | 44GB | 2×A100 / 4×RTX 4090 |
| Llama 3.3-70B | 144GB | 78GB | 43GB | 2×A100 / 4×RTX 4090 |
| DeepSeek-V3 (671B MoE) | ~370GB | ~200GB | ~110GB | 4-8×A100/H100 |

> 注意：DeepSeek-V3 虽是 671B 参数，但 MoE 架构每次推理只激活约 37B 参数，所以显存需求远比 671B 稠密模型低。

---

### 典型预算方案

#### 方案一：零成本起步（用 API）

| 项 | 内容 |
|---|---|
| 投入 | ¥0 |
| 用什么 | OpenAI API / DeepSeek API / 各种云厂商 API |
| 适合 | 实验阶段、不涉密数据、用量小 |
| 限制 | 数据出域，高频使用时成本不可控 |

#### 方案二：万元级（个人实验/小团队）

| 项 | 内容 |
|---|---|
| 预算 | ¥8,000 - 15,000 |
| GPU | 1× RTX 4090 24GB（约 ¥13,000）或 1× RTX 4060 Ti 16GB |
| 能跑什么 | 7B 全精度 / 14B INT4 / 32B INT4（勉强） |
| 推荐搭配 | Ollama + Qwen2.5:14b + Open WebUI |
| 适用 | 个人学习、小团队内部工具 |

#### 方案三：十万级（企业部门级）

| 项 | 内容 |
|---|---|
| 预算 | ¥50,000 - 150,000 |
| GPU | 1-2× A100 80G（¥8-12 万/张）或 2-4× RTX 4090 |
| 能跑什么 | 70B INT4 / 32B 全精度 / MoE 模型 |
| 推荐搭配 | vLLM + Qwen2.5-72B-AWQ |
| 适用 | 部门级内部服务，日均万次调用 |

#### 方案四：百万级（企业级生产集群）

| 项 | 内容 |
|---|---|
| 预算 | ¥500,000 - 2,000,000+ |
| GPU | 4-8× H100 80G / 4× H200 |
| 能跑什么 | 70B 全精度高并发 / MoE 全精度 / 支持微调 |
| 推荐搭配 | vLLM Ray 版 + 多节点 K8s 部署 |
| 适用 | 全公司 AI 平台，高并发，SLA 保障 |

#### 方案五：信创方案（国产 GPU）

| 项 | 内容 |
|---|---|
| GPU | 昇腾 910B（64GB）/ 寒武纪 MLU590 / 壁仞 BR100 |
| 能跑什么 | 7B-14B，成熟度有限 |
| 工具链 | MindSpore / 各家自研框架 |
| 限制 | 生态不如 CUDA 成熟，部分模型需适配移植 |

---

### 显存估算公式

#### 推理显存公式

```
推理总显存 = 模型权重显存 + KV Cache 显存 + 框架开销

模型权重显存 = 参数量(B) × 每参数字节数

KV Cache 显存 = 层数 × 2 × 每 token 的 KV 大小 × 上下文长度 × 并发数
               ≈ 参数量(B) × 0.5 × 上下文长度(token) × 并发数 × 量化系数
```

#### 快速估算口诀

```
INT4 推理：70B → 40G，7B → 5G（另加 1-3G 上下文开销）
FP16 推理：直接按 1B=2GB 算，70B≈140GB
微调所需：推理的 3-5 倍（梯度 + 优化器状态 + 激活值）
```

#### 实际样例计算

**场景：部署 Qwen2.5-72B-AWQ（INT4），支持 4096 token 上下文，并发 10**

```
模型权重：72B × 0.5B = 36GB
KV Cache（估）：~12GB（随并发线性增长）
框架开销：~2GB
总计：约 50GB → 1× A100 80G 可以跑，但并发 10 有压力
保守方案：2× A100 80G 做张量并行
```

**场景：微调 Llama 3.3-70B（LoRA）**

```
模型权重（BF16）：70B × 2B = 140GB
优化器状态（AdamW）：140GB × 2 = 280GB
梯度：140GB
激活值（批大小 × 序列长度）：~30-50GB
总计：约 580-600GB → 需要 8× A100 80G
如果用量化 LoRA（QLoRA）：可以用 4-bit 基础模型，降到 2-3× A100
```

---

## 3.2 硬件选型策略

### 需求驱动决策

做选型时，按以下顺序问自己：

```
1. 跑什么模型？（决定最低显存）
     ↓
2. 多少人在用？（决定并发和吞吐）
     ↓
3. 延迟要求？（实时对话 vs 批量处理）
     ↓
4. 数据能出网吗？（私有化 vs API）
     ↓
5. 预算多少？（最终拍板）
```

#### 第一问：模型选择 → 最低显存门槛

| 目标模型 | 最低显存（INT4） | 推荐显存（有余量） |
|---|---|---|
| 7B 级别 | 6GB | 12-16GB |
| 14B 级别 | 10GB | 20-24GB |
| 32B 级别 | 22GB | 40-48GB |
| 70B 级别 | 43GB | 80-160GB |
| MoE 大模型 | 100GB+ | 200-600GB |

#### 第二问：并发与吞吐

| 并发用户 | GPU 建议 | 推理框架 |
|---|---|---|
| 1-3 人（调试/自用） | 单卡即可 | Ollama / llama.cpp |
| 10-50 人（小团队） | 1-2 卡 | vLLM / TGI |
| 50-200 人（部门） | 2-4 卡 + 负载均衡 | vLLM Ray 版 |
| 200+ 人（全公司） | 4-8 卡集群 | vLLM + K8s + LB |

**经验值**：1× A100 80G 跑 70B-INT4，单并发约 15-25 token/s；跑 7B-FP16 可达 50-80 token/s。读者可根据这个比例线性外推。

#### 第三问：延迟要求

| 场景 | 可接受延迟 | 策略 |
|---|---|---|
| 实时对话 | < 1-2s 首 token | 小模型 / 大模型量化 / 多卡 |
| 代码补全 | < 500ms | 必须小模型或投机采样 |
| 文档摘要 | 10-30s 可接受 | 大模型 + 长上下文，可接受等待 |
| 批量评估 | 分钟级 | 最大化吞吐，不关心单次延迟 |

#### 第四问：数据合规

```
能上公有云吗？
├── 能 → 优先考虑 API，运维成本最低
└── 不能 → 必须私有化部署
            ├── 有现有机房吗？
            │   ├── 有 → 自购 GPU 服务器
            │   └── 没有 → 租 IDC 托管 / 私有云 GPU 实例
            └── 需要国产化吗？
                ├── 是 → 昇腾/寒武纪 + 适配工具链
                └── 否 → NVIDIA 方案
```

---

### 典型企业场景案例

#### 场景 A：创业公司智能客服（50 人团队）

```
需求：内部知识库问答，日均 500 次调用，实时响应
选型：
  模型：Qwen2.5-14B-INT4（10GB 显存）
  硬件：1× RTX 4090（24GB）
  框架：Ollama + Open WebUI（小团队够用）
  成本：硬件 ~¥13,000 一次性
  备选：如果并发上来了，升级 vLLM + A100 云实例
```

#### 场景 B：金融机构合规审查

```
需求：合同/文档审核，数据绝对不能出域，日均 5000 次
选型：
  模型：Qwen2.5-72B-AWQ（43GB）+ DeepSeek-V3 做复杂推理
  硬件：2× A100 80G（跑 72B）+ 4× H100（跑 DeepSeek）
  框架：vLLM + 自建 K8s 集群
  成本：硬件 ~¥80-120 万
  关键点：所有流量走内网，模型权重加密存储
```

#### 场景 C：政务信创场景

```
需求：公文撰写辅助，全链路国产化
选型：
  模型：Qwen2.5-32B（华为昇腾已适配 Qwen 系列）
  硬件：2-4× 昇腾 910B（64GB × 4 = 256GB 总显存）
  框架：MindSpore / 昇腾 CANN + vLLM（昇腾适配版）
  成本：昇腾约 ¥15-20 万/卡
  关键点：确认模型在昇腾上的适配状态，提前做 POC
```

#### 场景 D：互联网公司代码助手

```
需求：内部代码补全和审查，日均 10 万次调用，延迟 < 500ms
选型：
  代码补全（低延迟）：
    模型：Qwen3-Coder-7B-FP16
    硬件：2× RTX 4090（双卡做张量并行，保证延迟）
  代码审查（高质量）：
    模型：DeepSeek-V3（MoE，推理能力强）
    硬件：4× H100（或直接调 DeepSeek API）
  总成本：硬件 ~¥100 万 + API 费用
  框架：vLLM Ray 版 + 统一的 API 网关做路由
```

#### 选型决策速查表

| 场景 | 调用量 | 模型 | GPU | 月成本（电+折旧） |
|---|---|---|---|---|
| 个人学习 | 几十次/天 | 7B | RTX 4060 | ~¥100 |
| 小团队 | 几百次/天 | 14B | RTX 4090 | ~¥300 |
| 部门 | 几千次/天 | 32B-70B | 2× A100 | ~¥5,000 |
| 企业 | 几万次/天 | 70B + MoE | 4-8× H100 | ~¥20,000 |
| 大厂平台 | 百万次/天 | 混合多模型 | GPU 集群 | > ¥100,000 |

---

### 补充：推理框架选型速查

| 框架 | 适用场景 | 硬件要求 | 特点 |
|---|---|---|---|
| **Ollama** | 个人/小团队，快速体验 | 单卡，macOS/Linux | 一条命令部署，GGUF 生态 |
| **llama.cpp** | CPU/GPU 混合，边缘设备 | 极低，甚至纯 CPU | 灵活性最高，性能极致优化 |
| **vLLM** | 生产环境 API 服务 | 单卡到多节点集群 | PagedAttention，吞吐最高 |
| **TGI (Text Generation Inference)** | HuggingFace 生态 | 单卡/多卡 | HuggingFace 官方，易用 |
| **SGLang** | 复杂 LLM 程序 | 单卡/多卡 | 结构化生成，RadixAttention |
| **LMDeploy** | 国产，中英双语 | 单卡/多卡，支持昇腾 | TurboMind 引擎，持续批处理 |
| **TensorRT-LLM** | NVIDIA 深度优化 | NVIDIA GPU | 性能极致，但配置复杂 |

> 运维建议：**开发测试用 Ollama，生产环境用 vLLM**。这是目前最主流且经验证的模式。
