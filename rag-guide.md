# RAG（检索增强生成）详解

> 创建时间: 2026-05-06

---

## 什么是 RAG

### 一句话定义

**RAG（Retrieval-Augmented Generation，检索增强生成）** = 在让大模型回答问题之前，先从一个外部知识库里检索出相关资料，把这些资料连同问题一起喂给模型，让模型「参考着资料」回答。

### 运维类比

大模型就像一个运维新人，你问他「上次数据库故障怎么处理的」，他如果刚入职肯定不知道。但如果你先给他递过去那天的**故障复盘文档**（检索），他看完再回答你，答案就靠谱了。RAG 就是在做这件事——**先查资料，再回答**。

### 为什么需要 RAG

大模型有三大硬伤，RAG 恰好能补：

| 硬伤 | RAG 怎么解决 |
|---|---|
| **知识截止日期**：训练数据停在某个时间点 | 知识库可以实时更新，检索到最新信息 |
| **幻觉/瞎编**：碰到不知道的就编 | 强制参考文档回答，有据可查 |
| **私有知识盲区**：不知道你公司的内部系统 | 把内部文档导入知识库，模型就能「知道」了 |
| **黑盒不可追溯**：不知道模型为什么这么回答 | 可以返回引用的源文档，可审计 |

---

## RAG 核心架构

### 整体流程

```
                    离线阶段（建库）
┌────────────────────────────────────────┐
│                                         │
│  文档 → 切块 → Embedding → 向量数据库   │
│                                         │
└────────────────────────────────────────┘

                    在线阶段（问答）
┌────────────────────────────────────────┐
│                                         │
│  用户问题 → Embedding → 向量检索        │
│                         ↓               │
│                    相关文档片段          │
│                         ↓               │
│                  问题 + 片段 → LLM → 回答│
│                                         │
└────────────────────────────────────────┘
```

### 三大组件

| 组件               | 作用                         | 技术选型                                   |
| ---------------- | -------------------------- | -------------------------------------- |
| **Embedding 模型** | 把文本变成向量（一串数字），语义相近的文本向量也相近 | BGE/M3E/GTE/text-embedding-3 等         |
| **向量数据库**        | 存储向量 + 做相似度检索              | Milvus/Qdrant/Chroma/Weaviate/Pinecone |
| **LLM**          | 根据检索到的资料生成最终答案             | 任意大模型                                  |

---

## RAG 工作流程详解

### 离线阶段：构建知识库

#### 第 1 步：文档加载与解析

```
支持的数据源：
├── 纯文本（.txt）
├── Markdown（.md）
├── PDF（.pdf）  ← 注意：扫描版 PDF 需要 OCR
├── Word（.docx）
├── HTML / 网页
├── 代码文件（.py, .go, .yaml...）
├── 数据库记录
├── Confluence / 语雀 / 飞书文档（API 拉取）
└── 聊天记录、工单记录
```

关键问题：**文档解析质量直接决定 RAG 效果上限**。PDF 里的表格、图片、双栏排版是解析难点。

#### 第 2 步：文档切块（Chunking）

大模型有上下文长度限制，而且塞入太长的文档会稀释关键信息，所以要把文档切成合适的小块。

**切块策略对比**：

| 策略         | 做法                         | 适合场景     |
| ---------- | -------------------------- | -------- |
| **固定大小切块** | 每 512 token 一块，重叠 50 token | 通用，最简单   |
| **按句子切块**  | 按句号/换行自然断开                 | 叙事性文档    |
| **按段落切块**  | 保持段落完整                     | 结构化文档    |
| **语义切块**   | 用 Embedding 检测语义边界         | 效果最好但最慢  |
| **父子切块**   | 小块检索 + 大块喂给 LLM            | 兼顾精度和上下文 |

**切块大小的权衡**：

```
小块（128-256 token）：
  优点：检索更精准，能精确命中相关段落
  缺点：可能丢失上下文，看不懂代词指的是什么

大块（512-1024 token）：
  优点：上下文完整，不丢失信息
  缺点：检索精度下降，可能混入无关内容

推荐起步值：512 token，重叠 50 token
```

**实际例子**：

```
原文：
"Kubernetes 集群于 2024-03-15 发生故障。故障原因是 etcd 磁盘空间满。
运维团队通过扩容 etcd 存储解决。后续增加了磁盘监控告警。"

切块示意（真实场景 512 token 能装下整段，这里故意用极小的块演示"重叠"机制）：
  Chunk 1: "Kubernetes 集群于 2024-03-15 发生故障。故障原因是 etcd 磁盘空间满。"
  Chunk 2: "故障原因是 etcd 磁盘空间满。运维团队通过扩容 etcd 存储解决。后续增加了磁盘监控告警。"
  （Chunk 1 和 Chunk 2 重叠了一句，保证跨 chunk 的语义连续性）
```

#### 第 3 步：向量化（Embedding）

每个 chunk 被 Embedding 模型转换成一个固定维度的浮点数向量。

```
"Redis 主从切换故障排查" 
    → Embedding 模型 
    → [0.023, -0.451, 0.782, ..., 0.134]  # 1024 维或更多
```

语义相近的文本，向量在空间中距离就近；语义无关的文本，向量距离就远。这就是「语义搜索」的基础。

#### 第 4 步：存入向量数据库

向量数据库专门做一件事——在海量向量中**快速找到最相似的那几个**。

---

### 在线阶段：问答流程

```
用户问题: "上次 Redis 故障怎么解决的？"
    │
    ├── 1. 问题向量化（用同一个 Embedding 模型）
    │      "上次 Redis 故障怎么解决的？" → [0.031, -0.412, ...]
    │
    ├── 2. 向量检索（Top-K，通常取 3-10 个最相似 chunk）
    │      向量数据库返回：
    │      Chunk 42: "2024-03-15 Redis 主从切换故障复盘..." (相似度 0.93)
    │      Chunk 87: "Redis 常见故障处理手册..." (相似度 0.87)
    │      Chunk 15: "Redis sentinel 配置最佳实践..." (相似度 0.82)
    │
    ├── 3. 构建 Prompt（问题 + 检索到的文档 + 指令）
    │
    └── 4. 发给 LLM 生成答案
```

#### 第 3 步中构建的 Prompt 模板示例

```markdown
你是一个运维专家助手。请**只根据以下参考资料**回答用户问题。
如果参考资料中无法找到答案，请明确说「根据现有资料无法确定」，不要编造。

## 参考资料
[文档1]
2024-03-15 Redis 主从切换故障复盘：
故障时间：2024-03-15 14:32
根因：主节点 RDB 持久化导致 fork 子进程占满内存，触发 OOM Killer
解决：调整 maxmemory 策略为 allkeys-lru，关闭 RDB 改为 AOF
...

[文档2]
Redis 常见故障处理手册：
1. 连接超时：检查 maxclients 配置...
...

[文档3]
Redis sentinel 配置最佳实践：
down-after-milliseconds 建议 5000-10000...

## 用户问题
上次 Redis 故障怎么解决的？

## 你的回答
```

---

## Embedding 模型选型

### 什么是 Embedding 模型

和生成式大模型不同，Embedding 模型只做一件事——**把文本变成向量**。它的输出是一个固定长度的高维浮点数数组，不是文字。

### 主流 Embedding 模型

| 模型 | 维度 | 中文能力 | 特点 | 适用场景 |
|---|---|---|---|---|
| **BGE-M3** | 1024 | ★★★★★ | 国产最强，支持多语言，8192 上下文 | **中文首选** |
| **BGE-large-zh** | 1024 | ★★★★★ | 中文专精，轻量 | 纯中文场景 |
| **M3E-large** | 1024 | ★★★★ | 国产开源，社区活跃 | 性价比之选 |
| **Qwen3-Embedding** | 1024/2560/4096（支持 MRL 降维） | ★★★★★ | 阿里 2025 出品，0.6B/4B/8B 三档，多语言榜前列 | 中英混合、追新 |
| **GTE-Qwen2** | 1536（1.5B 版）/ 3584（7B 版） | ★★★★★ | 阿里上一代，仍很能打 | 长文本检索 |
| **jina-embeddings-v3** | 1024 | ★★★★ | 支持任务特定 LoRA | 多语言通用 |
| **text-embedding-3-large** | 3072 | ★★★ | OpenAI 官方，效果好 | 海外场景，用 API |
| **Cohere Embed v3** | 1024 | ★★★ | 企业级，分类任务强 | 海外商业场景 |

### 如何选择

```
中文为主 → BGE-M3（综合最强）或 M3E（轻量）
中英混合 → BGE-M3 或 GTE-Qwen2
纯 API 调用 → text-embedding-3-large
追求最新 → jina-embeddings-v3
```

> 运维建议：**先 BGE-M3 起步**，当前中文 RAG 场景的共识之选。之后可以根据效果和延迟再调。

---

## 向量数据库选型

### 核心概念

向量数据库的本质就是做**向量的近似最近邻搜索（ANN）**——在海量向量中快速找到和查询向量最相似的那些。

### 主流向量数据库对比

| 数据库               | 部署方式          | 规模   | 特点                      | 推荐场景             |
| ----------------- | ------------- | ---- | ----------------------- | ---------------- |
| **Chroma**        | 嵌入 / 独立       | 小-中  | Python 原生，API 最简单，零配置   | 原型验证、小项目         |
| **Qdrant**        | 自部署 / 云       | 中-大  | Rust 编写，性能好，过滤功能强       | **生产推荐**         |
| **Milvus**        | 分布式 / 单机      | 大-超大 | 云原生，分布式架构，支持十亿级向量       | 大规模企业级           |
| **Weaviate**      | 自部署 / 云       | 中-大  | 自带 Embedding 能力，GraphQL | 需要 Hybrid Search |
| **Pinecone**      | 仅云            | 大-超大 | 全托管，省心，按量付费             | 不想运维数据库          |
| **Elasticsearch** | 自部署           | 中-大  | 已有 ES 的团队，8.0+ 支持向量     | 已有 ES 基础设施       |
| **FAISS**         | 库（非数据库）       | 小-中  | Meta 出品，纯库，无持久化         | 离线计算、单机          |
| **PGVector**      | PostgreSQL 插件 | 小-中  | 集成在 PG 里，不用新系统          | 已有 PG 的团队        |

### 选型决策

```
你的阶段：
├── 刚接触 RAG，想跑通流程 → Chroma（Python 三行代码搞定）
├── 小团队生产环境 → Qdrant（性能好，维护简单）
├── 大规模企业级 → Milvus（分布式，支持十亿级）
├── 不想管数据库 → Pinecone / Zilliz Cloud（托管服务）
└── 已有 ES/PG 基础设施 → ES 向量插件 / PGVector（复用）
```

---

## 高级 RAG 技术

基础 RAG 能跑通，但在实际场景中经常不够用。以下是进阶技术：

### 1. 检索质量优化

| 技术                      | 做法                                 | 什么情况下用          |
| ----------------------- | ---------------------------------- | --------------- |
| **Hybrid Search（混合检索）** | 关键词检索（BM25）+ 语义检索（向量）取并集           | 需要精确匹配术语/ID/命令名 |
| **Reranking（重排序）**      | 先检索 Top-20，再用 Reranker 模型精排取 Top-3 | 检索精度是瓶颈时        |
| **Multi-Query**         | 把用户问题改写为多个不同角度的查询分别检索              | 用户问题太简短或模糊      |
| **HyDE**                | 先生成一个假设答案，再用这个答案去检索                | 问题和文档之间的语义鸿沟太大  |

### 2. 召回率提升

| 技术             | 做法                                      |
| -------------- | --------------------------------------- |
| **父子切块**       | 用小块（128 token）做检索，返回大块（1024 token）给 LLM |
| **小到大检索**      | 检索目标块 + 其前后相邻块一起返回                      |
| **句子窗口检索**     | 每个句子单独嵌入，检索后还原上下文窗口                     |
| **知识图谱 + RAG** | 文档先抽取实体关系建图，检索时同时查图和向量                  |

### 3. 答案质量提升

| 技术 | 做法 |
|---|---|
| **Self-RAG** | 模型先判断「需要检索吗？」，检索后再判断「每个片段相关吗？」 |
| **Corrective RAG** | 检索后评估相关性，不相关就自动改写查询重新检索 |
| **ReAct / Agentic RAG** | 模型可以多步检索：查资料→发现不够→再查→再回答 |

### 4. 索引层优化

| 技术 | 做法 |
|---|---|
| **增量索引** | 知识库更新时只重新向量化新增文档 |
| **多级索引** | 先做粗粒度检索（章节级），再细粒度检索（段落级） |
| **元数据过滤** | 结合 tag、日期、来源过滤，缩小检索范围 |
| **分库检索** | 按领域拆库（故障库、配置库、脚本库），精准路由 |

---

## RAG vs 微调：什么时候用哪个

这是新人最容易困惑的问题，这里彻底讲清楚：

| 维度 | RAG | 微调 |
|---|---|---|
| **核心能力** | 给模型外挂知识库 | 改变模型的行为模式 |
| **知识更新** | 实时（更新知识库即可） | 需重新训练 |
| **新知识注入** | ★ 最佳方案 | 中等（可能遗忘旧知识） |
| **风格/格式固化** | 靠 prompt，不稳定 | ★ 最佳方案 |
| **领域术语掌握** | 中（术语在文档中就能检索到） | ★ 最佳（内化到模型参数） |
| **幻觉控制** | ★ 有效（有源文档约束） | 中等 |
| **可解释性** | ★ 高（可追溯源文档） | 低（参数学到的，说不清） |
| **实现成本** | 低（小时级上线） | 高（需 GPU + 数据 + 训练） |
| **维护成本** | 低（更新文档即可） | 高（需重训来更新知识） |
| **推理成本** | 高（多了检索 + 长 prompt） | 低（模型本身更小或更精准） |
| **实时性要求** | 可以有毫秒级检索延迟 | 无额外延迟 |

### 组合使用

很多情况下 RAG 和微调**不是二选一，而是组合**：

```
方案一：先 RAG 后微调
  用 RAG 快速上线 → 收集用户真实问题 → 用这些数据做微调

方案二：微调 + RAG
  微调模型学会领域术语 + 输出格式
  RAG 提供实时检索的外部知识
  一个解决「怎么说」，一个解决「说什么」

方案三：RAG 代理微调模型
  用小模型 + 领域微调做路由/分类
  用 RAG + 大模型做复杂推理
```

---

## 运维场景的 RAG 实战架构

### 一个可落地的运维知识库方案

```
┌─────────────────────────────────────────────────┐
│                    数据源                         │
│  故障复盘 │ 运维文档 │ 告警记录 │ 脚本仓库 │ 工单  │
└──────────────────┬──────────────────────────────┘
                   ↓
┌──────────────────────────────────────────────────┐
│              文档处理流水线                        │
│  1. 文档解析（PDF/Word/Markdown/Confluence API）  │
│  2. 文本清洗（去敏感信息、统一格式）                │
│  3. 智能切块（按文档类型选切块策略）                │
│  4. 向量化（BGE-M3）                              │
└──────────────────┬───────────────────────────────┘
                   ↓
┌──────────────────────────────────────────────────┐
│              向量数据库（Qdrant/Milvus）           │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐             │
│  │ 故障库   │ │ 配置库   │ │ 脚本库   │            │
│  └─────────┘ └─────────┘ └─────────┘             │
└──────────────────┬───────────────────────────────┘
                   ↓
┌──────────────────────────────────────────────────┐
│               检索层                               │
│  Hybrid Search + Reranking + 元数据过滤            │
└──────────────────┬───────────────────────────────┘
                   ↓
┌──────────────────────────────────────────────────┐
│              LLM 生成层                            │
│  构建 Prompt → 调用模型 → 后处理 → 返回             │
│  支持引用源文档、置信度标注                         │
└──────────────────────────────────────────────────┘
```

### 最小可行方案（今天就能跑）

```python
# minimal_rag.py — 最小可运行的 RAG 示例
import chromadb
from openai import OpenAI

# 1. 准备知识库
documents = [
    "2024-03-15 Redis 主从切换故障：根因是 RDB 持久化导致 OOM，解决：改用 AOF",
    "生产环境 Nginx 配置规范：worker_processes 设为 CPU 核数，worker_connections 1024",
    "数据库连接池最佳实践：连接数 = (CPU核数 × 2) + 可用磁盘数",
]

# 2. 建向量库
client = chromadb.Client()
collection = client.create_collection("ops_knowledge")
for i, doc in enumerate(documents):
    collection.add(documents=[doc], ids=[str(i)])

# 3. 问答
question = "Redis 故障怎么处理？"
results = collection.query(query_texts=[question], n_results=2)
context = "\n\n".join(results["documents"][0])

prompt = f"""参考以下文档回答问题，无法确定请说明：

## 参考文档
{context}

## 问题
{question}

## 回答"""

# 4. 调用 LLM（这里以 OpenAI 为例，可换成任意 API；
#    注意别复用上面 chromadb 的 client 变量名）
llm = OpenAI()
response = llm.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": prompt}],
)
print(response.choices[0].message.content)
```

---

## RAG 效果评估

### 核心指标

| 指标                             | 含义                | 目标值         |
| ------------------------------ | ----------------- | ----------- |
| **召回率 (Recall)**               | 检索到的相关文档 / 所有相关文档 | > 90%       |
| **精确率 (Precision)**            | 检索结果中真正相关的比例      | > 80%       |
| **MRR (Mean Reciprocal Rank)** | 第一条相关结果排在第几位      | 越靠前越好       |
| **答案准确率**                      | 生成的答案是否正确         | > 85%（业务场景） |
| **幻觉率**                        | 答案包含编造信息的比例       | < 5%        |
| **引用忠实度**                      | 答案是否忠于引用的文档，而非发散  | > 90%       |

### 评估框架

- **RAGAS**：开源 RAG 评估框架，自动评测检索和生成质量
- **DeepEval**：另一个 RAG 评测工具，指标更丰富
- **TruLens**：可视化 RAG 链路，支持中间步骤调试

---

## 常见坑与解决

| 坑                   | 现象                      | 解决                             |
| ------------------- | ----------------------- | ------------------------------ |
| **切块太大**            | 检索不精准，答案发散              | 缩小 chunk size，用父子切块            |
| **切块太小**            | 检索到片段太碎，丢失上下文           | 加大 chunk size，或用小到大检索          |
| **Embedding 模型不匹配** | 中文场景用英文 Embedding 模型    | 换 BGE-M3 或 M3E                 |
| **检索不到**            | 召回率低，总是漏掉相关文档           | 试试 Hybrid Search + Multi-Query |
| **检索到不相关**          | 精确率低，噪音太多               | 加 Reranking，提高检索阈值             |
| **长文档截断**           | 文档超过 max_seq_length 被截掉 | 预处理时切块，或用长上下文模型                |
| **答案与文档不一致**        | 模型自行发挥，不忠实于文档           | 强化 prompt 约束，用 Self-RAG        |
| **Embedding 更新不及时** | 加新文档后检索不到               | 确认向量库的 upsert 逻辑是否正确           |
